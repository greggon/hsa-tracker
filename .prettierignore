# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Vendored from the Claude Design project — kept byte-comparable to upstream
# so it can be re-synced without a reformat diff.
src/lib/styles/nocturne.css
