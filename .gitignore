node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/data
# Safety copies left by the backup and pull scripts. Same contents as /data —
# receipts and all — so they must never be committable.
/data.local-*
/data.restored-*
/data.broken-*

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
